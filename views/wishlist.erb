<div class="header">
  <h1>My Wishlist</h1>
</div>


<% @item.each do |item| %>
  <p>Item: <%= item.name %></p>
  <p>Price: $<%= item.price %></p>
  <img src="<%= item['pic'] %>" alt="My Item">
<% end %>
